# 2026-06-13

- Shifted project direction from direct coding-agent CLI wrapping to ACP-backed agent sessions.
- Updated `AGENTS.md`, `README.md`, and `MEMORY.md` to make communication threads the authoritative agent session context.
- Added `coderclaw.acp.AgentClient`, typed agent session request/message models, and adapted the orchestrator to call `execute_session`.
- Kept the Codex CLI path as a legacy adapter behind the new ACP-shaped boundary.
- Changed Slack context collection to fetch thread context instead of neighboring channel history.
- Installed Homebrew Python 3.11, created `.venv`, installed `.[dev]`, and verified `python -m pytest` passes with 16 tests.
