# Daily Memory

This directory stores append-oriented daily memory files in the format:

```text
memory/daily/YYYY-MM-DD.md
```

Intended use:

- short-term notes
- current work context
- temporary reminders
- observations that may later be promoted into `MEMORY.md`

The main curated long-term memory lives in the repository root `MEMORY.md`.

