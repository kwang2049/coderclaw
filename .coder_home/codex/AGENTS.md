# Codex Home Instructions

This `AGENTS.md` lives inside the project-local `CODEX_HOME` used by CoderClaw.

Use it for Codex-specific steering that should apply whenever CoderClaw invokes the Codex CLI for this repository.

Primary rules:

- respect the repository root `AGENTS.md`
- keep changes local-first and auditable
- prefer shared project conventions over user-machine-specific assumptions

